# FRONTEND-ONLY CONVEX DEPLOYMENT (Separate from backend to avoid WebSocket conflicts)
# To create a new deployment: npx convex dev --new-deployment
VITE_CONVEX_URL=https://brave-aardvark-510.convex.cloud

# ASTRO SHIVA ENGINE (The API)
VITE_ASTRO_API_URL=https://astro-shiva-app.vercel.app/api/v1

# NOTE: Frontend uses HTTP API only - Convex is used for real-time subscriptions only
# Backend uses separate Convex connection for database operations